/**
 * @deprecated Button2 está deprecado. Importa el Button unificado:
 *   import { Button } from "@/components/ui/Button";
 *
 * Se mantiene como re-export para evitar romper imports legacy.
 * Este archivo será eliminado cuando todos los consumidores migren.
 */
import { Button } from "./Button";
export default Button;
